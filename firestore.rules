rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Leaderboard: anyone can read and write (public game)
    match /leaderboard/{slug} {
      allow read, write: if true;
    }
  }
}
